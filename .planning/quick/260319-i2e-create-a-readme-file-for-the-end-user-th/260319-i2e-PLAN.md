---
phase: quick
plan: 260319-i2e
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "A developer reading the README understands what Starter.WebApi is, its core value proposition (modular removability), and all included features"
    - "The README provides clear quickstart instructions (clone, configure secrets, run)"
    - "Each module is listed with a brief description and its extension method name so developers know how to add/remove features"
    - "Configuration guidance covers appsettings.json sections, User Secrets, and environment variables"
  artifacts:
    - path: "README.md"
      provides: "End-user documentation for the starter repository"
      min_lines: 150
  key_links: []
---

<objective>
Create a comprehensive README.md for the Starter.WebApi repository that describes all features, modules, and usage patterns for end users (developers who will clone and customize this starter).

Purpose: The repository has no README. Developers cloning this repo need to understand what it includes, how to get started, how modules work, and how to add/remove features.
Output: A single README.md at the repository root.
</objective>

<execution_context>
@F:/Personal/bootstrapper-apps/web-api/.claude/get-shit-done/workflows/execute-plan.md
@F:/Personal/bootstrapper-apps/web-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/milestones/v1.0-ROADMAP.md
@src/Starter.WebApi/Program.cs
@src/Starter.WebApi/appsettings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with full feature documentation</name>
  <files>README.md</files>
  <action>
Create README.md at the repository root with the following sections. Use the project context, requirements, and appsettings.json as the source of truth for all details.

**Structure:**

1. **Title and tagline** — "Starter.WebApi" with one-line description: a modular .NET 10 Web API starter where every feature is independently removable.

2. **Core Value** — Explain the modular composition pattern: each feature is a class library with `AddStarter{Module}` / `UseStarter{Module}` extension methods. Removing a feature = delete the extension method call in Program.cs + remove the project reference. No cascading breakage.

3. **Features** — Organized by concern (matching Program.cs grouping). For each feature area, list what is included:
   - **Observability**: Serilog structured logging with two-stage bootstrap, configurable sinks (Console, File, Seq, OpenTelemetry/App Insights), request logging with correlation IDs
   - **Security**: ASP.NET Identity user store, JWT Bearer tokens, Google OAuth (all independently removable), PolicyScheme routing
   - **Data**: EF Core 10 with SQLite default, swappable to SQL Server/PostgreSQL via config, separate migration assemblies per provider, repository pattern
   - **API Surface**: OpenAPI 3.1 with Scalar interactive docs, API versioning (URL segment, v1/v2 samples), FluentValidation with Problem Details, CORS (dev/prod profiles)
   - **Production Hardening**: Rate limiting (fixed window, sliding window, token bucket), in-memory + distributed caching (Redis-swappable), Gzip/Brotli response compression (opt-in), standardized API response envelope (opt-in)
   - **Error Handling**: Global exception handler with RFC 7807 Problem Details, stack traces in dev only
   - **Health Checks**: /health, /health/ready, /health/live endpoints, DB connectivity check, sample external service check
   - **Testing**: Integration tests (WebApplicationFactory), unit tests, architecture tests (NetArchTest module isolation), module removal smoke tests (19 modules)

4. **Prerequisites** — .NET 10 SDK

5. **Quick Start** — Step-by-step:
   - Clone the repo
   - Set up User Secrets for JWT key: `dotnet user-secrets set "Jwt:SecretKey" "your-256-bit-secret-key-here-min-32-chars" --project src/Starter.WebApi`
   - Run: `dotnet run --project src/Starter.WebApi`
   - Visit Scalar API docs (mention the default URL)
   - SQLite database is auto-created, no setup needed

6. **Project Structure** — Show the solution layout with all 21 src projects and 3 test projects. Group by concern. Use a tree-style listing.

7. **Configuration** — Explain that all runtime behavior is in appsettings.json via IOptions<T>. List the configuration sections briefly (Database, Jwt, Authentication:Google, Cors, OpenApi, RateLimiting, Caching, Compression, HealthChecks, Serilog) with a one-liner each. Point to appsettings.json for full documentation.

8. **Database Providers** — How to switch: change `Database:Provider` to "Sqlite", "SqlServer", or "PostgreSql" and update the connection string. Mention migration helper scripts in /scripts.

9. **Adding and Removing Modules** — This is the key differentiator. Show a concrete example of removing a module (e.g., removing Google OAuth):
   - Delete `builder.AddAppGoogle();` from Program.cs
   - Remove the project reference to Starter.Auth.Google
   - Build succeeds, app runs without Google auth
   Show that ALL modules follow this same pattern.

10. **Module Reference** — A table listing every module, its extension method(s), and the appsettings section it uses (if any). Include all 19+ removable modules:
    - Starter.Auth.Shared (AddAppAuthShared)
    - Starter.Auth.Identity (AddAppIdentity)
    - Starter.Auth.Jwt (AddAppJwt)
    - Starter.Auth.Google (AddAppGoogle)
    - Starter.Caching (AddAppCaching)
    - Starter.Compression (AddAppCompression / UseAppCompression)
    - Starter.Cors (AddAppCors)
    - Starter.Data (AddAppData / UseAppData)
    - Starter.ExceptionHandling (AddAppExceptionHandling / UseAppExceptionHandling)
    - Starter.HealthChecks (AddAppHealthChecks / UseAppHealthChecks)
    - Starter.Logging (AddAppLogging / UseAppRequestLogging)
    - Starter.OpenApi (AddAppOpenApi / UseAppOpenApi)
    - Starter.RateLimiting (AddAppRateLimiting / UseAppRateLimiting)
    - Starter.Responses (AddAppResponses)
    - Starter.Validation (AddAppValidation)
    - Starter.Versioning (AddAppVersioning)

11. **Running Tests** — Commands: `dotnet test` from root. Briefly describe the three test projects and what they cover.

12. **Configuration Secrets Guidance** — Development (User Secrets), Environment Variables (double-underscore nesting), Production (Azure Key Vault or env vars). Keep it concise, referencing the comments already in appsettings.json.

13. **License** — Add an MIT license placeholder section (just a note saying "MIT" or link to LICENSE file -- do NOT create a LICENSE file unless one exists).

**Style guidelines:**
- Write for developers who will clone and customize this repo
- Be concise but thorough -- this is reference documentation, not a tutorial
- Use code blocks for commands and file paths
- Use tables where they improve scanability (module reference especially)
- Do NOT use emojis
- Keep the tone professional and direct
  </action>
  <verify>
    <automated>test -f F:/Personal/bootstrapper-apps/web-api/README.md && wc -l F:/Personal/bootstrapper-apps/web-api/README.md | awk '{if ($1 >= 150) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>README.md exists at the repository root with 150+ lines covering: project description, core value proposition, all features organized by concern, quickstart instructions, project structure, configuration reference, database provider switching, module add/remove instructions with example, module reference table, test instructions, and secrets guidance.</done>
</task>

</tasks>

<verification>
- README.md exists at repository root
- All 16+ modules are listed with their extension method names
- Quickstart section has runnable commands
- Module removal example is concrete and accurate
- Configuration sections match what is actually in appsettings.json
</verification>

<success_criteria>
A developer cloning this repo for the first time can read the README and understand: what the project is, how to run it, what features are included, how to configure them, and how to add/remove modules. All information is accurate to the actual codebase.
</success_criteria>

<output>
After completion, create `.planning/quick/260319-i2e-create-a-readme-file-for-the-end-user-th/260319-i2e-SUMMARY.md`
</output>
